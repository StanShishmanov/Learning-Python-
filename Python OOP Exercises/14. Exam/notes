To gather statistics about "The System" - a network of components connected to build something which functions locally,
but I don't need to know that.
- I need to build a program which processes statistics about the System